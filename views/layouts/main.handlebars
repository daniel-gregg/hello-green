<!DOCTYPE html>
<html lang="en">

<head>
    <title>Hello Green</title>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link href="https://unpkg.com/tailwindcss@^2/dist/tailwind.min.css" rel="stylesheet">
    <link href="/css/style.css" rel="stylesheet">
</head>
<body>
    <header>
        <nav class="flex items-center justify-between flex-wrap bg-teal p-6">
            <div class="flex items-center flex-no-shrink text-white mr-6">
                <a href="/" class="font-semibold text-xl tracking-tight">Hello Green project reporting templates</a>
            </div>
            <div class="w-full block flex-grow lg:flex lg:items-center lg:w-auto">
                <div class="text-sm lg:flex-grow">
                {{#if loggedIn}}<a href="/dashboard" class="block mt-4 lg:inline-block lg:mt-0 text-teal-lighter hover:text-white mr-4">
                    Dashboard
                </a>{{/if}}
                {{#unless loggedIn}}<a href="/login" class="block mt-4 lg:inline-block lg:mt-0 text-teal-lighter hover:text-white mr-4">
                    Login
                </a>{{/unless}}
                {{#if loggedIn}}<a href="/logout" class="block mt-4 lg:inline-block lg:mt-0 text-teal-lighter hover:text-white">
                    Logout
                </a>{{/if}}
                </div>
            </div>
        </nav>
    </header>
    {{{body}}}
</body>
</html>